@page
@model Bargio.Areas.Admin.Pages.EditDatabase.RaccourcisProms.IndexModel

@{
    ViewData["Title"] = "Index";
    Layout = "~/Pages/Shared/_LayoutAdmin.cshtml";
}

<h2>Index</h2>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.PromsKeyboardShortcut[0].TBK)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PromsKeyboardShortcut[0].Proms)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.PromsKeyboardShortcut) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TBK)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Proms)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Raccourci">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Raccourci">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Raccourci">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
